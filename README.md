# browserify-react-boilerplate


One terminal tab (livereload):

```
npm run watchify
```

Another terminal tab (server):

```
npm run nodemon
```

Also (compiling for production usage):

```
npm run browserify
```
